# 批处理


### 代码实现

```python
import numpy as np

network = init_network()

(train_img, train_label), (test_img, test_label) = load_mnist()

correct = 0
batch_size = 100

# 循环所有图片
for i in range(0, len(test_img), batch_size):
    # 分割图片集，[0, 100), [100, 200), [200, 300) ...
    test_img_batch = test_img[i:i + batch_size]
    # 输出：(100, 784)
    print(test_img_batch.shape)

    y_batch = predict(network, test_img_batch)
    # 输出：（100, 10）
    print(y_batch.shape)

    # 获取概率最高的元素索引，axis 指定从第二维度找最大值索引
    # [[1, 2, 3], [6, 5, 4], [7, 9, 8]] 会输出：[2, 0, 1]
    p = np.argmax(y_batch, axis=1)

    # 判断推理的是否正确
    correct += np.sum(p == test_label[i:i + batch_size])

print("correct/all: {}/{}".format(correct, len(test_img)))
```

# sigmoid 函数


### sigmoid 函数

* 公式

h(x) = 1 / (1 + exp(-x))
exp(-x) 表示 e 的 -x 方


* 代码实现

```python
import numpy as np


# 支持 NumPy 数组
def sigmoid(x):
    # np.exp(-x) 就表示 e 的 -x 方
    return 1 / (1 + np.exp(-x))


x = np.array([-1.0, 1.0, 2.0])
y = sigmoid(x)
print(y)
```

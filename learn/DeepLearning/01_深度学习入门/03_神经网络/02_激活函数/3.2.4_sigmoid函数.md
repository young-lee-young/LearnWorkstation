# sigmoid 函数


### 作用

把任意实数映射到 0 - 1，正规化数据


### 公式

$$
h(x) = \frac{1}{1 + e^{-x}}
$$


### 代码实现

```python
import numpy as np


# 支持 NumPy 数组
def sigmoid(a):
    # np.exp(-x) 就表示 e 的 -x 方
    return 1 / (1 + np.exp(-a))


x = np.array([-1.0, 1.0, 2.0])
y = sigmoid(x)
print(y)
```
